#!/usr/bin/env sh
##############################################################################
# Gradle start up script for UN*X
# Auto-generated minimal wrapper script
##############################################################################
basedir=$(dirname "$0")
if [ -z "$GRADLE_HOME" ]; then
  GRADLE_HOME="${basedir}/.gradle/gradle"
fi
exec "${GRADLE_HOME}/bin/gradle" "$@"
